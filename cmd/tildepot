#!/bin/bash
#
# tildepot
# A command line tool to manage your home setup, including applications,
# dotfiles, preferences, and more.

# shellcheck source-path=../

# Enable strict mode
set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/../src/app.sh"
source "$(dirname "${BASH_SOURCE[0]}")/../src/cmd/tildepot.sh"
